# Superset of every app's requirements, plus the test tooling. Each app keeps its
# own requirements.txt for deployment; this file exists so CI can install once.
shiny>=0.6.0
numpy>=1.24
scipy>=1.10
pandas>=2.0
matplotlib>=3.7
statsmodels>=0.14
pytest>=8.0
